#include <stdio.h>

int ksor(int num, int resul) {

    while(num > 0) {
        num ^= resul;
        
    }
}

int main {

    unsigned int a;

    printf("Enter integer: \n");
    scanf("%u", a);

    int result = 0;

    wh
}